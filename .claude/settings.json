{
  "slashCommands": {
    "/commit": {
      "description": "Create a git commit with AI-generated message based on diff. Include the standard Claude Code footer in commit messages",
      "command": "git add -A && git status && git diff --cached"
    }
  }
}
